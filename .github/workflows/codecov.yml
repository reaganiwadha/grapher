name: codecov

on:
  push:
    branches: [ "trunk" ]
  pull_request:
    branches: [ "trunk" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Codecov
      uses: codecov/codecov-action@v3.1.1

